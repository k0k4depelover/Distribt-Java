server:
  port: 9001

spring:
  application:
    name: msvc-suscriptions

  datasource:
    url: jdbc:postgresql://postgres:5432/suscriptions-db
    username: admin
    password: s3cur3p4SSw0rd


  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka


