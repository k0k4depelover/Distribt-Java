server:
  port: 9001

spring:
  application:
    name: msvc-suscribtions

  datasource:
    url: jdbc:postgresql://postgres-subs:5432/subs_db
    username: admin
    password: s3cur3p4SSw0rd

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka


